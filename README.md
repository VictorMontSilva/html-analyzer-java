# html-analyzer-java
Focused code designed to be an HTML page/structure error analyzer

It is a OOP code in java designed to analyze pages and return a message of error in case any malformation in the HTML was detected.

This version works with JDk 17 or newer, and was done locally via VSCode
